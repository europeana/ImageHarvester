The client currently supports S3 as the storage place
In order to enable that modify

media-storage-type = S3

and

media-storage {
    client_key = "client_key"
    secret_key = "secret_key"
    region = "region"
    bucket = "bucket"
}